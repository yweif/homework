# 回家作业系统需求说明书

## 1. 引言

### 1.1 编写目的
本文档旨在详细描述回家作业管理系统的功能需求、非功能需求及系统架构，为开发团队提供清晰的开发指导。

### 1.2 项目背景
随着教育信息化的推进，传统的纸质作业管理模式已无法满足现代学习需求。本系统旨在为学生提供一个便捷的回家作业管理平台，支持多端访问，实现作业的数字化管理。

### 1.3 参考资料
- 软件工程需求分析标准
- RESTful API 设计规范
- Docker 容器化部署最佳实践

## 2. 系统概述

### 2.1 系统目标
构建一个支持 Web 端和 iOS App 端的回家作业管理系统，实现作业的数字化管理、状态跟踪和时间记录。

### 2.2 系统构成
| 组成部分 | 说明 |
|---------|------|
| 服务器端 | 提供 API 服务，处理身份认证和业务逻辑 |
| Web 端 | 基于浏览器的管理界面，提供作业看板和 CRUD 功能 |
| iOS App 端 | 苹果手机应用，提供作业查询和状态更新功能 |

### 2.3 系统架构
采用 B/S（Browser/Server）架构，服务器端提供 RESTful API，Web 端和 App 端通过 API 与服务器交互。系统支持容器化部署，便于扩展和维护。

```
┌─────────────────┐      ┌─────────────────┐
│   Web 浏览器    │      │   iOS App       │
└────────┬────────┘      └────────┬────────┘
         │                        │
         └───────────┬────────────┘
                     │ HTTPS/RESTful API
                     ▼
         ┌───────────────────────┐
         │   服务器端 (Docker)    │
         │  - 用户认证           │
         │  - 业务逻辑           │
         │  - 数据管理           │
         └───────────┬───────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │      数据库           │
         └───────────────────────┘
```

## 3. 功能需求

### 3.1 服务器端功能需求

#### 3.1.1 用户认证模块
- **功能描述**：支持 Web 端和 App 端的用户登录验证
- **功能要点**：
  - 支持用户名/密码登录
  - 采用 JWT（JSON Web Token）进行身份验证
  - Token 有效期管理
  - 支持多点登录控制

#### 3.1.2 API 接口模块
- **功能描述**：提供 RESTful API 供各端调用
- **功能要点**：
  - 用户管理接口（登录、注册、信息修改）
  - 学科管理接口（增删改查）
  - 作业管理接口（增删改查、状态更新）
  - 时间记录接口（开始时间、完成时间记录）

#### 3.1.3 数据管理模块
- **功能描述**：管理作业、学科、用户等核心数据
- **功能要点**：
  - 用户信息管理
  - 学科信息管理
  - 作业信息管理
  - 作业状态跟踪
  - 时间记录管理

#### 3.1.4 容器化部署
- **功能描述**：支持 Docker 容器化部署
- **功能要点**：
  - 提供 Dockerfile 用于构建镜像
  - 提供 docker-compose.yml 用于多容器编排
  - 支持环境变量配置
  - 支持数据持久化

### 3.2 Web 端功能需求

#### 3.2.1 用户登录
- **功能描述**：支持用户通过 Web 端登录系统
- **输入**：用户名、密码
- **输出**：登录成功跳转至主页面，失败提示错误信息

#### 3.2.2 作业看板
- **功能描述**：整体展示所有回家作业的状态概览
- **功能要点**：
  - 以卡片或列表形式展示所有作业
  - 显示作业总数、未完成数、已完成数等统计信息
  - 支持按状态筛选（全部/未开始/进行中/已完成）
  - 支持按学科筛选

#### 3.2.3 状态跟踪
- **功能描述**：跟踪每个回家作业的状态
- **状态定义**：
  - 未开始：作业已创建但未开始
  - 进行中：用户已开始做作业
  - 已完成：用户已完成作业
- **功能要点**：
  - 每个作业显示当前状态
  - 状态以不同颜色或图标区分
  - 支持状态变更的日志记录

#### 3.2.4 学科分类管理
- **功能描述**：按学科对作业进行分类展示和管理
- **功能要点**：
  - 支持预设学科（如：语文、数学、英语、物理、化学等）
  - 支持学科的新增、修改、删除
  - 按学科查看作业列表

#### 3.2.5 作业添加
- **功能描述**：支持添加新的回家作业
- **输入字段**：
  - 学科（下拉选择）
  - 作业标题
  - 作业内容/描述
  - 截止时间
  - 备注（可选）
- **功能要点**：
  - 表单验证
  - 添加成功后刷新作业列表

#### 3.2.6 作业删除
- **功能描述**：支持删除指定的回家作业
- **功能要点**：
  - 删除前需二次确认
  - 删除后刷新作业列表
  - 记录删除操作日志

#### 3.2.7 作业查询
- **功能描述**：支持按条件查询作业
- **查询条件**：
  - 按学科筛选
  - 按状态筛选
  - 按关键词搜索（标题/内容）
  - 按时间范围筛选（创建时间/截止时间）
- **功能要点**：
  - 支持组合查询
  - 查询结果分页展示

#### 3.2.8 作业修改
- **功能描述**：支持编辑已有作业信息
- **功能要点**：
  - 点击作业进入编辑页面
  - 支持修改所有字段（学科、标题、内容、截止时间等）
  - 修改后保存并刷新列表

### 3.3 iOS App 端功能需求

#### 3.3.1 用户登录
- **功能描述**：支持用户通过 App 登录系统
- **输入**：用户名、密码
- **输出**：登录成功进入主页面，失败提示错误信息
- **功能要点**：
  - 支持记住登录状态
  - 支持登出功能

#### 3.3.2 学科查询
- **功能描述**：按学科分类查看作业列表
- **功能要点**：
  - 首页展示学科列表
  - 点击学科进入该学科下的作业列表
  - 作业列表显示作业标题、状态、截止时间等基本信息

#### 3.3.3 开始作业
- **功能描述**：点击"开始"按钮，记录开始时间
- **操作流程**：
  1. 在作业详情页点击"开始"按钮
  2. 系统记录当前时间为开始时间
  3. 作业状态变更为"进行中"
- **功能要点**：
  - 只有状态为"未开始"的作业才能点击开始
  - 开始时间不可修改

#### 3.3.4 完成作业
- **功能描述**：点击"完成"按钮，记录完成时间
- **操作流程**：
  1. 在作业详情页点击"完成"按钮
  2. 系统记录当前时间为完成时间
  3. 作业状态变更为"已完成"
- **功能要点**：
  - 只有状态为"进行中"的作业才能点击完成
  - 完成时间不可修改
  - 可查看作业时长（完成时间 - 开始时间）

#### 3.3.5 时间记录
- **功能描述**：自动记录作业开始时间和完成时间
- **记录内容**：
  - 开始时间：用户点击"开始"时的时间戳
  - 完成时间：用户点击"完成"时的时间戳
  - 作业时长：完成时间与开始时间的差值
- **功能要点**：
  - 时间精确到分钟
  - 支持查看历史作业的时间记录

## 4. 非功能需求

### 4.1 性能需求
- API 响应时间：普通查询 < 500ms，复杂查询 < 2s
- 支持并发用户数：至少 100 用户同时在线
- 数据库查询优化，建立必要索引

### 4.2 安全性需求
- 用户密码采用 bcrypt 等安全算法加密存储
- API 接口需进行身份验证（JWT Token）
- 敏感数据（如密码）禁止明文传输
- 防止 SQL 注入、XSS 等常见安全攻击

### 4.3 可用性需求
- Web 端支持主流浏览器（Chrome、Firefox、Safari、Edge）
- App 端支持 iOS 14.0 及以上版本
- 系统界面简洁直观，操作流程清晰

### 4.4 可扩展性需求
- 支持后续增加新学科
- 支持后续增加新功能模块（如作业提醒、作业评分等）
- 数据库设计预留扩展字段

### 4.5 可维护性需求
- 代码遵循统一的编码规范
- 关键功能添加注释
- API 接口提供文档说明

### 4.6 容器化需求
- 服务器端支持 Docker 容器化部署
- 提供完整的 Dockerfile 和 docker-compose.yml
- 支持通过环境变量配置系统参数
- 支持数据卷挂载实现数据持久化

## 5. 数据需求

### 5.1 数据实体

#### 5.1.1 用户（User）
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | 主键 | 用户唯一标识 |
| username | 字符串 | 用户名 |
| password_hash | 字符串 | 密码哈希值 |
| created_at | 时间戳 | 创建时间 |
| updated_at | 时间戳 | 更新时间 |

#### 5.1.2 学科（Subject）
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | 主键 | 学科唯一标识 |
| name | 字符串 | 学科名称 |
| description | 字符串 | 学科描述（可选） |
| created_at | 时间戳 | 创建时间 |

#### 5.1.3 作业（Homework）
| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | 主键 | 作业唯一标识 |
| user_id | 外键 | 所属用户 |
| subject_id | 外键 | 所属学科 |
| title | 字符串 | 作业标题 |
| content | 文本 | 作业内容/描述 |
| status | 枚举 | 状态：未开始/进行中/已完成 |
| deadline | 时间戳 | 截止时间 |
| start_time | 时间戳 | 开始时间（可为空） |
| complete_time | 时间戳 | 完成时间（可为空） |
| created_at | 时间戳 | 创建时间 |
| updated_at | 时间戳 | 更新时间 |

### 5.2 数据关系
- 一个用户可以有多个作业（一对多）
- 一个学科下可以有多个作业（一对多）
- 作业属于一个用户和一个学科（多对一）

## 6. 技术选型建议

### 6.1 服务器端
| 技术栈 | 选项 | 说明 |
|-------|------|------|
| 开发语言 | Node.js / Python / Java | 任选，建议 Node.js（Express）或 Python（FastAPI） |
| Web 框架 | Express / FastAPI / Spring Boot | 根据开发语言选择 |
| 数据库 | MySQL / PostgreSQL / MongoDB | 关系型数据库推荐 MySQL 或 PostgreSQL |
| 认证方式 | JWT | JSON Web Token |
| 容器化 | Docker + Docker Compose | 容器化部署 |

### 6.2 Web 前端
| 技术栈 | 选项 | 说明 |
|-------|------|------|
| 前端框架 | React / Vue / Angular | 任选，建议 React 或 Vue |
| UI 组件库 | Ant Design / Element UI / Material-UI | 根据框架选择 |
| 状态管理 | Redux / Vuex / Pinia | 可选 |
| HTTP 客户端 | Axios | API 请求 |

### 6.3 iOS App
| 技术栈 | 选项 | 说明 |
|-------|------|------|
| 开发语言 | Swift | 苹果官方推荐 |
| UI 框架 | SwiftUI / UIKit | 建议 SwiftUI（现代、简洁） |
| 网络请求 | URLSession / Alamofire | 可选 |
| 数据持久化 | Core Data / UserDefaults | 根据需求选择 |

## 7. 接口定义（示例）

### 7.1 用户登录
```
POST /api/auth/login
Request:
{
  "username": "string",
  "password": "string"
}
Response:
{
  "token": "string",
  "user": {
    "id": "number",
    "username": "string"
  }
}
```

### 7.2 获取作业列表
```
GET /api/homework?subject_id=1&status=进行中
Headers:
  Authorization: Bearer {token}
Response:
{
  "data": [
    {
      "id": "number",
      "title": "string",
      "subject": "string",
      "status": "string",
      "deadline": "string",
      "start_time": "string",
      "complete_time": "string"
    }
  ],
  "total": "number"
}
```

### 7.3 创建作业
```
POST /api/homework
Headers:
  Authorization: Bearer {token}
Request:
{
  "subject_id": "number",
  "title": "string",
  "content": "string",
  "deadline": "string"
}
Response:
{
  "id": "number",
  "message": "作业创建成功"
}
```

### 7.4 更新作业状态
```
PATCH /api/homework/{id}/status
Headers:
  Authorization: Bearer {token}
Request:
{
  "status": "进行中",
  "start_time": "string"
}
Response:
{
  "message": "状态更新成功"
}
```

## 8. 验收标准

### 8.1 服务器端
- [ ] 用户登录验证功能正常
- [ ] RESTful API 接口符合规范
- [ ] 支持 Docker 容器化部署
- [ ] 数据持久化正常

### 8.2 Web 端
- [ ] 用户登录功能正常
- [ ] 作业看板展示正确
- [ ] 支持按学科分类管理
- [ ] 作业的增删改查功能完整
- [ ] 状态跟踪功能正常

### 8.3 iOS App 端
- [ ] 用户登录功能正常
- [ ] 按学科查询作业功能正常
- [ ] "开始作业"功能正常，记录开始时间
- [ ] "完成作业"功能正常，记录完成时间
- [ ] 时间记录准确

## 9. 项目里程碑（建议）

| 阶段 | 任务 | 预计周期 |
|------|------|---------|
| 第一阶段 | 需求分析、系统设计、技术选型 | 1 周 |
| 第二阶段 | 服务器端开发（API + 数据库） | 2 周 |
| 第三阶段 | Web 端开发 | 2 周 |
| 第四阶段 | iOS App 端开发 | 2 周 |
| 第五阶段 | 系统集成测试、Bug 修复 | 1 周 |
| 第六阶段 | 部署上线、用户培训 | 1 周 |

## 10. 附录

### 10.1 术语表
- **B/S 架构**：Browser/Server（浏览器/服务器）架构
- **RESTful API**：一种网络 API 设计风格
- **JWT**：JSON Web Token，用于身份验证的开放标准
- **Docker**：容器化平台，用于应用的打包和部署

### 10.2 修订历史
| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|---------|
| v1.0 | 2026-05-07 | - | 初始版本 |

---

**文档结束**
